Recreate my existing website as accurately as possible:

SOURCE WEBSITE:
https://shivamgroupofenterprises.framer.website

IMPORTANT:
Do NOT ask me for screenshots. You must inspect the live website directly from the URL and use it as the primary visual reference.

First, thoroughly inspect the entire public website before writing the final code.

TASK:

1. Crawl and inspect ALL accessible pages and routes of the website, including:

   * Home
   * About
   * Services
   * Contact
   * Any other linked pages
   * Navbar/dropdown links
   * Footer links

2. Analyze the existing website's visual design in detail:

   * Exact/approximate background colors and HEX values
   * Primary, secondary and accent colors
   * Typography and font style
   * Font sizes and font weights
   * Heading hierarchy
   * Page width and max-width
   * Section spacing
   * Padding and margins
   * Border radius
   * Borders and shadows
   * Buttons
   * Cards
   * Icons
   * Images and image placement
   * Navbar
   * Footer
   * Hero sections
   * Animations and hover effects
   * Gradients
   * Responsive behavior
   * Mobile/tablet/desktop layouts

3. Inspect the actual content of the website and preserve it:

   * Company name
   * Headings
   * Paragraphs
   * Services
   * Contact information
   * Navigation labels
   * Buttons
   * Any other visible text

4. Recreate the website with the SAME overall:

   * Visual hierarchy
   * Layout
   * Spacing
   * Color palette
   * Typography
   * Component style
   * Image style
   * Navigation
   * Footer
   * Responsive behavior

5. Do not create a generic template.
   The result should look like the SOURCE WEBSITE, not merely a website inspired by it.

6. Use reusable components and clean architecture.
   For example:

   * Navbar
   * Hero
   * Service cards
   * Sections
   * CTA
   * Footer
   * Buttons
   * Reusable typography/components

7. Make the website fully responsive:

   * Desktop
   * Laptop
   * Tablet
   * Mobile

8. Recreate animations/interactions where they are visible on the source website, but keep them smooth and performant.

9. If the original website uses images/logos that are publicly accessible, use the same assets where legally and technically possible. If an asset cannot be accessed, use the closest appropriate replacement and maintain the same dimensions, crop, aspect ratio and visual role.

10. Do not remove or simplify sections just because they are difficult to reproduce.

TECH STACK:
Use a modern React-based implementation suitable for Replit.
Use clean HTML semantics, CSS/Tailwind where appropriate, and reusable React components.

SEO:
Preserve the important page titles, headings and content structure from the original website.

FINAL REQUIREMENT:
After building the website, compare your implementation against the source URL and fix obvious differences in:

* spacing
* colors
* typography
* sizing
* alignment
* images
* responsive layout
* buttons
* navbar
* footer

Do not stop after creating only the homepage. Recreate ALL accessible pages.

Start by inspecting the source website first, then build the complete implementation.
